# ========================================
# CORE FASTAPI & WEB FRAMEWORK
# ========================================
fastapi==0.128.0
uvicorn[standard]==0.27.0
python-multipart==0.0.21
# NOTE: Do NOT pin starlette separately — FastAPI manages its own starlette version.

# ========================================
# DATABASE (MongoDB)
# ========================================
motor==3.7.1
pymongo==4.15.5

# ========================================
# AUTHENTICATION & SECURITY
# ========================================
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
# NOTE: PyJWT removed — python-jose is the sole JWT library used in this project.
cryptography==46.0.3
argon2-cffi==23.1.0
email-validator==2.1.0
phonenumbers==8.13.26

# ========================================
# LLM & AI FRAMEWORKS
# ========================================
langchain==0.2.11
langchain-core==0.2.23
langchain-community==0.2.10
langchain-groq==0.1.6
langgraph==0.1.5
groq==0.37.1
openai==1.7.2

# ========================================
# DOCUMENT PROCESSING
# ========================================
# OCR - Text extraction from images
pytesseract==0.3.13
Pillow==10.2.0

# PDF processing
PyPDF2==3.0.1

# Word document processing
python-docx==1.2.0

# ========================================
# AUDIO PROCESSING
# ========================================
pydub==0.25.1

# Speech Recognition - Voice Input Support (file-based only, no microphone)
SpeechRecognition==3.10.0

# NOTE: openai-whisper, torch, torchaudio REMOVED from production dependencies.
# Whisper is only a local fallback — the primary path uses Groq Cloud API.
# openai-whisper pulls ~2GB of torch dependencies, making Docker builds
# extremely slow and memory-intensive. The backend audio_service.py and
# gradio_app.py both use try/except to gracefully handle missing whisper.
# For local development with Whisper fallback, install separately:
#   pip install openai-whisper torch torchaudio

# ========================================
# VALIDATION & SETTINGS
# ========================================
pydantic==2.12.5
pydantic-settings==2.1.0
# NOTE: Do NOT pin pydantic_core separately — pydantic manages its own core version.
python-dotenv==1.0.0

# ========================================
# HTTP & REQUESTS
# ========================================
httpx==0.25.2
requests==2.31.0

# ========================================
# GRADIO UI
# ========================================
gradio==6.2.0
gradio_client==2.0.2

# ========================================
# TESTING (Development only — not installed in production Docker)
# ========================================
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# ========================================
# TYPE CHECKING
# ========================================
typing_extensions==4.15.0